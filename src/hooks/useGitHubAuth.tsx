import { GitHubAppProvider, useGitHubApp } from "@/contexts/GitHubContext";

/**
 * @deprecated Use GitHubAppProvider from @/contexts/GitHubContext instead.
 * This file is kept for backward compatibility and will be removed once migrations are complete.
 */
export { GitHubAppProvider, useGitHubApp };

// Re-exporting this as the default or similar if needed, 
// but based on current usage it's usually named exports.
